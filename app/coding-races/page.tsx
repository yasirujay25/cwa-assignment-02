"use client";

export default function CodingRaces() {
  const containerStyle: React.CSSProperties = {
    padding: "20px",
    textAlign: "center",
  };

  const titleStyle = {
    fontSize: "28px",
    fontWeight: "bold",
    marginBottom: "15px",
  };

  return (
    <div style={containerStyle}>
      <h1 style={titleStyle}>Coding Races</h1>
      <p>Coming Soon...</p>
    </div>
  );
}
